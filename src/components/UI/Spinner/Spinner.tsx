import * as React from 'react';
import { FC } from 'react';

const Spinner:FC = () => {
  return (
    <div className='spinner_inner'><span className='spinner'/></div>
  );
};

export default Spinner;
